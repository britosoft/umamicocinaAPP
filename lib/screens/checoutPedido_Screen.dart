import 'package:flutter/material.dart';

class checoutPedido_Screen extends StatelessWidget {
  const checoutPedido_Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      height: double.infinity,
      color: Colors.green,
      child: Stack(
        children: [
          Text('Mi pedido'),
        ],
      ),
    );
  }
}
